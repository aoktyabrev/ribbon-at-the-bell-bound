# Лог решений (SPEC §9.3, CLAUDE.md)

Фиксируются отклонения от пре-регистрации, изменения анализа и «стопы».
Пре-регистрированные предсказания в experiments/*.yaml НЕ редактируются.

---

## 2026-07-06 — Фаза B, R1: несходимость + ферро-знак, СТОП перед R2

**Что произошло.** R1 (κ ∈ {0.1, 1, 10}, geodesic, T=0) прогнан по пре-регистрации.
Сырой результат при 6000→12000 шагах: κ=10 дал форму +cosθ (по форме p≈2), ВОПРЕКИ
пре-регистрации (ожидались пила/p≈1, p=2 не ожидался). Знак корреляции —
ферромагнитный: E(0)=+1 (концы выравниваются), а не −1 (синглет).

**Паранойя (SPEC §9.2, обязательна при p≈2).** Скрипты `paranoia_R1.py`,
`conv_study.py` → `results/R1/paranoia.md`, `results/R1/convergence_study.md`:
1. E(θ) при κ=10 ДРЕЙФУЕТ с числом шагов (|ΔE| 6k→24k = 0.35), круче к ступени.
2. Удвоение N,B меняет E на 0.58 (длинная цепь релаксирует медленнее — недосходимость).
3. Знак ферро — не баг: при θ=0 доля s==t = 0.975; классификатор верен.
4. Истинное равновесие: κ=1 сходится (~30k шагов) к неполной ферро-корреляции
   E≈0.38·cosθ (НЕ насыщается до ±1 ⇒ вне семейства хорды); κ=10 сходится (>100k
   шагов) к ферро-СТУПЕНИ при 90° (p→∞, PR-box). **p=2 нигде; это транзиент.**

**Вывод.** Пре-регистрированный энергетический критерий сходимости (|ΔE|/шаг<1e-6,
однократное удвоение) неадекватен: недостижим при k_e~сотни и допускает лишь 6k→12k
шагов, тогда как κ=10 требует >100k. Приведённые в `report.md` кривы R1 суть
транзиенты неполной релаксации; показатель p̂≈1.93 для κ=10 — артефакт.

**Решения (требуют подтверждения архитектора):**
- (анализ, применено) Фит закона хорды теперь ПЕРЕБИРАЕТ знак корреляции
  (`analysis.fit_chord_signed`): пре-регистрированный фит молча предполагал
  антиферро (E(0)=−1) и вырождался на границу сетки на ферро-данных. Это
  исправление вырождения, НЕ подгонка p (показатель падает куда падает). Пре-
  регистрация в yaml не тронута.
- (протокол, СТОП) R2 (термический) НЕ запущен: гонять отжиг поверх несошедшегося
  и неверно-понятого базиса = наука на битом конфиге (CLAUDE.md запрещает).
- Физика (для архитектора, НЕ интерпретирую сам): изотропная упругая лента фазы 1 —
  ФЕРРОМАГНЕТИК (выравнивает концы), а не синглет. Семейство хорды к ней неприменимо
  (κ=1 не насыщается; κ=10 → ступень). Возможные направления — анти-зажим,
  ограничение нулевой скрутки (R4) или спинорный режим (R5) для получения синглета.

**ARCH-QUESTION (см. сводку архитектору):** критерий сходимости и режим шагов;
ферро- vs синглетная постановка; смысл R2 до сходимости R1; фикс κ=0.1 (маргиналы
отклонены на ~3.6σ, устойчиво к steps — мелкий отдельный вопрос).

---

## 2026-07-06 — Решение архитектора: R5 ВНЕ ОЧЕРЕДИ (до R1b/R2)

Обоснование архитектора: критический тест спинорной структуры (различение q/−q,
720°-периодичность) важнее упорядоченного прохода. R2/R3/R4 не трогать.

**Внедрено по указанию:**
1. `energy.py`: третий режим `elastic: spinor` — геодезия БЕЗ модуля
   d(p,q)=arccos(clip(<p,q>)), различает q и −q. Тест: d(q,−q)=π (spinor) vs 0 (geodesic).
2. Новый критерий сходимости (ответ на ARCH-Q#1): БЛОЧНЫЙ по НАБЛЮДАЕМОЙ —
   `max_θ |ΔE| < max(2·σ_MC, 0.01)` между блоками, потолок 200k шагов
   (`dynamics.block_convergence`). Заменяет неадекватный энергетический |ΔE|<1e-6.
3. Диагностика кинков: счётчик пар <q_i,q_{i+1}> < 0 в финале (полуоборотные кинки
   на S³); плотность по θ/ячейкам + корреляция «кинки ↔ ветвь» (гипотеза: миноритарная
   ветвь несёт больше кинков).
4. R5 = 2×2 ячейки (elastic∈{geodesic,spinor} × κ∈{1,10}) для честного сравнения под
   ОДНИМ блочным протоколом. Пре-регистрация — `experiments/R5.yaml` (до запуска):
   (a) знак остаётся ферро; (b) кинки застревают при T=0, плотность>0;
   (c) амплитуда E(0) при κ=1 spinor НИЖЕ geodesic; (d) A3/A1 — открытый вопрос.

**Параллельно (дёшево):** R1b (свип N∈{2,4,8,16,32,64}, κ=1, geodesic, до сходимости —
гипотеза падения амплитуды 0.62→0.38); диагностика κ=0.1 (зеркало a→−a, антитетическая
инициализация n/−n, гистограмма |n_A·a|). Скрипты `r1b_Nsweep.py`, `margin_diag.py`.

После R5 — стоп, сводка архитектору.

---

## 2026-07-06 — Сверка с теорией архитектора (R1_theory_prediction.md)

Архитектор зафиксировал предсказание ДО данных (жёсткий предел + приведённая
двухфреймовая модель, k_eff=κ·k_c). Сверка с моими R1/R5/R1b:

- **Жёсткий предел κ→∞ = ступень E=sign(cosθ) (PR-box):** ✅ R5 κ=10 (geo и spin) →
  E=±1.000, ступень при 90°. Совпадает с §1.
- **Ферро-знак (§5):** ✅ найден независимо; я реализовал `fit_chord_signed` (перебор
  знака) — функционально эквивалентно директиве t̃=−t (E_singlet=−E_sim). Сырые счётчики
  хранятся в ферро-конвенции. Могу перевести на точную конвенцию t̃=−t, если нужно.
- **κ=10 A3/A1:** предсказано −0.317 (числ. приведённой модели), −1/3 у идеальной
  ступени; измерено −0.255 (N=64, сходимость 94%, ceiling-limited) — не дотянуто до
  ступени из-за потолка шагов на средних θ; тренд к −1/3 при полной сходимости.
- **КЛЮЧЕВОЕ отклонение — приведённая модель ≠ полная цепь (§6).** Предсказание E(0)|κ=1
  ≈ +0.62 — это ДВУХФРЕЙМОВЫЙ (N=2) предел. R1b: N=2 → E(0)=0.66 (✅ модель), но полная
  цепь НАСЫЩАЕТСЯ на 0.38 при N≥16. Интерьер НЕ сводится к одной пружине — динамический
  интерьер (память формы/твист-моды §6) — самостоятельный результат. Приведённая модель
  верна лишь при N=2.
- **Ножницы амплитуда–форма (§4):** ✅ подтверждено — ни одна ячейка не имеет
  одновременно |E(0)|=1 и гладкий косинус (κ=1: гладко, но 0.38; κ=10: |E|=1, но ступень).
- **R5-специфика:** спинор не двигает ножницы при T=0 (кинки разглаживаются, плотность
  0.500→0.000). Вопрос §4 «меняют ли ножницы положение в спиноре» — ответ: НЕТ при T=0.

---

## 2026-07-06 — Решения архитектора по итогам R5/R1b

1. **Конвенция:** точный глобальный флип t̃=−t (`analysis.singlet_counts`); фиты SPEC
   «как есть» в синглете; `fit_chord_signed` УДАЛЁН. Сырьё — в ферро, флип в анализе.
2. **κ=10:** ступень принята (теорема κ→∞ + тренд), ceiling НЕ поднимать.
3. **NO-GO зафиксирован в отчёте R5:** осевые наблюдаемые (функции n) факторизуются
   через SU(2)→SO(3) и гейдж-слепы к ℤ₂-слою; спинорность видна ТОЛЬКО через
   голономию h=sign(Π<q_i,q_{i+1}>). Отсюда R5b.
4. **R5b (главная линия):** термический спинор, κ=1, N=64, T0=1.0, decay∈{0.999,
   0.9995,0.9999}, блочная сходимость после T<1e-4. Диагностики: плотность кинков,
   распределение h, E(θ|h±), корреляция h↔ветвь. Пре-рег R5b.yaml (a-c).
5. **M2** — отдельный пре-регистрированный ВАРИАНТ МОДЕЛИ (не правка R5b): голономно-
   одетая t̃=h·sign(n_B·b), считается на тех же финалах. Честно: меняет определение
   наблюдаемой, сравнивать с M1 бок о бок.
6. **κ=0.1:** тест поворота — глобальная ротация (a,b) от координатных осей (3
   ориентации) + один прогон float64. Гипотеза: 5σ — анизотропия float32-сетки при
   осевыровненных зажимах. Antithetic не нужен. Таймбокс: не разрешится — парковать.

**Внедрено (код).** analysis: singlet_counts + флип, sign-машинерия удалена. dynamics:
run(step0) для отжига через блоки, holonomy(). experiment: run_cell_blocks считает
голономию/E(θ|h)/M2 и поддерживает отжиг с cold_T-гейтом; _render_r5b. R5b.yaml, R5-no-go.

**Раннее наблюдение (smoke R5b):** плотность кинков = 0 при ВСЕХ decay, включая
decay=0.9999 (T_fin≈0.45). Причина — энергия кинка ~k_e·arccos²≈155…620 (k_e=63) ≫ T0=1.0:
термическая энергия слишком мала, чтобы населить ℤ₂-сектор. Проверяем на полном прогоне.

**РЕЗУЛЬТАТЫ.** R5b полный (817с) подтвердил: плотность кинков = P(h=−1) = 0.0000 при
всех decay ⇒ (a) ОПРОВЕРГНУТ, h=+1 у всех, M2≡M1, E(θ|h−) не определена (c невозможно).
(b) ферро ✅. Побочно: амплитуда растёт с замедлением отжига 0.49→0.66→0.99 (отжиг ведёт
к аглайн-ступени, превью R2). decay=0.9999 (амп 0.99) — контроли падают (насыщение).
Спинорная линия закрыта: спинор динамически невидим при T0≤1.0.

**κ=0.1 РАЗРЕШЕНО (тест поворота, margin_rot_quick.py):** гипотеза архитектора
подтверждена. axis-aligned max|z|=4.57 (СМЕЩЕНИЕ, p=0.000); rot1 max|z|=3.03, rot2
max|z|=2.95 (оба В НОРМЕ, p>0.05). Поворот зажимов прочь от координатных осей убивает
5σ ⇒ это float32-анизотропия сетки при осевыровненных зажимах, НЕ физика. Численный
артефакт. (float64/CPU-контроль пропущен: чужой процесс python3 main.py периодически
занимает GPU на 100% и вешает прогоны; тест поворота уже решающий — таймбокс закрыт.)

---

## 2026-07-07 — Фаза B принята. Программа фазы C (решения архитектора)

- **Q1:** путь R3→R4; знако-чувствительный зажим ОТКЛОНЁН (постулировал бы видимость
  спинорной фазы вместо вывода).
- **Q2:** R2-лайт для формального закрытия пре-регистрации.
- **GPU:** тяжёлые прогоны согласовывать с окнами тренировочного процесса (python3 main.py).

Порядок: **R2-лайт → R3 → R4a → R4b → мягкая лента.** Стоп после каждого КРАСНОГО контроля.

- **R2-лайт:** κ=1 geodesic, T0=1, decay∈{0.999,0.9995,0.9999}, полный свип θ, фиты+A3/A1.
  Вердикт по гипотезе (амплитуда растёт, форма к ступени) — явной строкой.
- **R3 (Коссера):** относит. поворот r_i=q_i⁻¹⊗q_{i+1}, лог ω_i в материальном фрейме i;
  τ_i=ω_i·e_z (скрутка), изгиб — поперечные. E=Σ k_b(ω_x²+ω_y²)+k_t·τ². Свип k_t/k_b∈
  {0.1,1,10} при фикс. суммарной жёсткости (κ=1-эквивалент), T=0, блочный. Пре-рег: знак
  ферро; двигает ли анизотропия ножницы (амп/A3-A1) — открытый.
### R3 — КРАСНЫЙ КОНТРОЛЬ (стоп по протоколу)

R3 (Коссера, T=0, блочный) прогнан (4077с). **Физика драматична:** жёсткость ИЗГИБА
управляет ножницами — kt/kb=0.1 (жёсткий изгиб) → ступень амп=1.0; kt/kb=1 → 0.82;
kt/kb=10 (мягкий изгиб) → декуплирование амп=0.01. НО **контроли §4.2 КРАСНЫЕ:**
±-симметрия нарушена до 127σ (kt/kb=0.1), 92σ (изотроп); воспроизводимость падает
(19σ, 8σ) у жёстких ячеек; мягкая (kt/kb=10) почти чиста.

**Диагноз (не физика, численность):** нарушение ±-симметрии сосредоточено при θ=0
(осевыровненный зажим a=b=e_z), |P(pp)−P(mm)|≈0.67, СОГЛАСОВАННО по обоим сидам (не
метастабильность). Энергия Коссера ПРОВЕРЕНА ±-симметричной (инвариантна к флипу осей
q→q⊗j, как и geodesic). ⇒ симметричная энергия + симметричная Haar-инициализация →
асимметричный итог возможен только через УСИЛЕНИЕ float32-смещения нестабильной
динамикой у симметричного седла θ=0. Худшее у самых ЖЁСТКИХ/долгих релаксаций (200k
шагов), почти нет у мягкой — сигнатура совпадает с артефактом κ=0.1 (float32-анизотропия
осевыровненной сетки). Подтверждение — тест поворота зажимов (r3_diag.py, в работе):
поворот прочь от осей должен убить асимметрию.

**Проверенные фиксы (все ОТКЛОНЕНЫ данными):**
- Поворот зажимов (r3_symmetry_diag): асимметрия при θ=0 НЕ убирается — 0.663 (axis),
  0.807 (rot1), 0.192 (rot2). Не осевой артефакт как κ=0.1; величина зависит от пути.
- Отжиг T0=1 (r3_anneal_test): НЕ помогает — asym 0.660 (T=0) vs 0.674 (отжиг) при θ=0.
  Значит это не нестабильное седло, которое лечит шум.
- float64/CPU: нереалистично (таймаут 400с; fp64 на этом железе непригоден, как и 10ч
  зависание на GPU ранее).

**Итоговый диагноз:** ±-симметрия исходов РОБАСТНО нарушена в режиме Коссера (не лечится
поворотом/отжигом), при том что энергия ПРОВЕРЕНА ±-симметричной. Единственное
согласованное объяснение — float32-НЕэквивариантность ГРАДИЕНТА Коссера (многошаговый
путь conj→quat_mul→log_map/atan2 не коммутирует с q→q⊗j на уровне бит), усиленная у
неустойчивого выбора ±z при θ=0 и долгой жёсткой релаксацией (200k). Geodesic (простой
путь градиента arccos) симметричен — отсюда контраст. Худшее у самых жёстких ячеек.

**СТОП (протокол архитектора: стоп после красного контроля).** R4/R4a/R4b/мягкая лента
НЕ запускаются. Знак ферро подтверждён; изгибная жёсткость сильно двигает ножницы, но
без исправления ±-симметрии измерение недостоверно. Вопросы к архитектору: (i) float64
на GPU (нужна карта с нормальным fp64 или иной бэкенд); (ii) переписать градиент Коссера
в явно ±-эквивариантной форме (напр. энергия через инварианты Re(q_i⁻¹q_{i+1}) без
атан2-ветвления); (iii) исключить θ=0/180 (седло) и мерить только внутренние θ, приняв
концы-симметрию как отдельный контроль. Пре-регистрация R3 не тронута.

### R3-chordal (Вариант 2 архитектора) — ЭСКАЛАЦИЯ: диагноз float32 ОПРОВЕРГНУТ

Внедрён cosserat_chordal (energy E=Σ4k_b(x²+y²)+4k_t z² по компонентам r=q_i⁻¹⊗q_{i+1},
без atan2/log_map; τ̃=2z; Tw-машинерия переведена на 2z). Тесты §7: geodesic градиент
ПОБИТОВО эквивариантен (atol=0 ✅); cosserat_chordal НЕ побитово (1.9e-6: quat_mul
суммирует 4 члена в фикс. порядке ⇒ перестановка при флипе даёт ULP-шум), но ошибка
НЕСМЕЩЁННАЯ; atan2-Коссера — xfail.

**Мини-fp64 (шаг 3) ОПРОВЕРГ пре-регистрированный диагноз:** старый atan2-Коссера даёт
±-асимметрию **22.63σ во ФЛОАТ32 И ФЛОАТ64 ОДИНАКОВО** (N=16,B=256,kt/kb=0.1). fp64
проверен (qf.dtype=float64). Т.е. асимметрия atan2-Коссера ДЕТЕРМИНИРОВАНА и
НЕЗАВИСИМА ОТ ТОЧНОСТИ ⇒ это РЕАЛЬНЫЙ дефект формулировки log_map, а НЕ float32.
Мой прежний диагноз (float32-неэквивариантность) НЕВЕРЕН.

**Загадка:** энергия И градиент atan2-Коссера ±-эквивариантны на СЛУЧАЙНЫХ конфигах в
fp64 (maxΔ~1e-13), но РЕЛАКСАЦИЯ ломает симметрию (22.63σ) — эквивариантность рвётся
вдоль траектории (на конфигах, встречаемых при жёсткой релаксации; механизм не
локализован, флип w≥0 и atan2 к энергии/градиенту через ω² инвариантны).

**Фикс cosserat_chordal ВАЛИДИРОВАН независимо:** при θ=0 даёт |P(pp)−P(mm)|=0.025,
P(s+)=0.492 (симметрично) против 0.73 у atan2. Т.е. РЕМЕДИ архитектора работает —
но по ДРУГОЙ причине (реальная разница формулировок, не точность).

**СТОП + ЭСКАЛАЦИЯ (правило архитектора: если fp64 не убивает — стоп, диагноз неверен).**
R3-chordal полный и R4 НЕ запускаю без решения архитектора. Вопрос: (a) принять
chordal (симметрия валидна) и гнать R3-chordal, несмотря на неверный прежний диагноз?
(b) или сперва локализовать реальный дефект atan2 (напр. проверить, рвётся ли
эквивариантность градиента на РЕЛАКСИРОВАННЫХ, а не случайных, конфигах)?
Машинерия/тесты chordal готовы и зелёные; xfail документирует atan2-дефект.

### R3-chordal — тоже КРАСНЫЙ + (b)-lite итог (2026-07-07)

Путь (a)+(b)-lite выполнен. Тест-политика поправлена (побитово atol=0 только geodesic;
chordal — несмещённость: |mean знакового сдвига по орбите|<1e-9, ULP-перестановки quat_mul
приняты). 51 тест зелёный.

**(a) R3-chordal полный (619с): КРАСНЫЙ КОНТРОЛЬ.** Контроли §4.2 ❌: ±-симметрия до 18σ
(kt/kb=1), маргиналы ❌ (P(s+)~0.48). НО физика СОВСЕМ ДРУГАЯ: амплитуда ≈0 у ВСЕХ ячеек
(0.011/0.019/0.019) — chordal-энергия НАСЫЩАЕТСЯ на больших углах (|r_vec|≤1 ограничен) ⇒
слишком МЯГКАЯ пружина ⇒ концы ДЕКУПЛИРУЮТСЯ ⇒ ножницы не измерить. Сверка старое/новое:
chordal ОПРОВЕРГАЕТ atan2-выводы (изгиб→ступень, амп=1.0) — те были артефактом
неограниченной жёсткости atan2 (+ баг асимметрии). Поворот зажимов НЕ убирает chordal-
смещение маргиналов (0.016 axis vs rotated) — не float32-сетка как κ=0.1, а декуплированные
концы слабо пришпилены ⇒ систематический ~2% сдвиг значим при B=16384.

**ИТОГ: НИ ОДНА формулировка Коссера не даёт чистых контролей.** atan2 — верная жёсткость,
но реальный ±-баг (fp64-подтверждён); chordal — симметрична-дружелюбна, но слишком мягкая
(декупл, амп≈0) + остаточный маргинальный сдвиг. Гейт R4 НЕ открыт.

**(b)-lite (таймбокс):** шаг 1 — флип w≥0 (единственная знаковая ветвь, на r НЕ q) НЕ
виновник: он ±-эквивариантен (сопряжение сохраняет r_w), и его удаление УХУДШАЕТ симметрию.
Канонизации q·sign(w_q) в коде НЕТ. Шаг 2 — бисекция градиента: на СЛУЧАЙНЫХ конфигах
grad-эквивариантность держится ~1e-13 (fp64), но CPU-fp64-релаксация для поиска первого
расходящегося шага НЕ УЛОЖИЛАСЬ в бюджет (fp64 на CPU слишком медленный + GPU занят R3).
Механизм atan2-дефекта НЕ локализован. Атан2-Коссера помечен deprecated (xfail-тест).

**СТОП. Вопрос архитектору:** Коссера-энергия нужна ОДНОВРЕМЕННО жёсткой (растёт с углом,
как atan2), ±-симметричной по исходам, и с хорошими контролями. Кандидаты: (i) хордальная
с БÓЛЬШИМ множителем/иным ядром, не насыщающимся так рано; (ii) atan2 но с явной
±-симметризацией меры/классификатора; (iii) гибрид: geodesic-жёсткость (работает!) +
анизотропия через проекцию соседней оси, минуя лог-отображение; (iv) иное. R4a-диагностика
меры добавлена в конфиг (до запуска, ждёт разблокировки гейта).

### R3-geo — ЗЕЛЁНЫЙ (наконец) + физика подтверждена (2026-07-07)

cosserat_geo (E=k_b·d²+(k_t−k_b)·d²·твист-доля) + зеркальные пары. Прогон 889с, 3 ячейки, 2 сида.
- **ПРИЁМКА энергии ✅:** ρ=1 vs geodesic κ=1: max_θ|ΔE|=0.020, max|z|=**1.90σ < 2σ**. cosserat_geo
  изотропный воспроизводит зелёный R1/R5.
- **ФИЗИКА (чисто, ферро, зеркальные контроли зелёные):** амплитуда max|E| по k_t/k_b:
  0.1→**1.000 (ступень)**, 1→**0.377 (≈geodesic)**, 10→**0.023 (декупл)**. **ЖЁСТКОСТЬ ИЗГИБА
  УПРАВЛЯЕТ НОЖНИЦАМИ** — вывод atan2 ПОДТВЕРЖДЁН на чистой энергии; chordal-декуплирование
  было артефактом мягкости. Изгиб передаёт выравнивание концов, скрутка — нет.
- **Контроли:** зеркальный |E_h1−E_h2| ВСЕ ✅ (0.45/0.10/0.14 ≪ 3.08); воспроизв. (сиды)
  0.1✅ 1✅, 10 — 3.19σ (маргинально > 3.08 на ДЕКУПЛИРОВАННОЙ ячейке амп≈0; шум: Бонферрони
  α=0.05 ⇒ ~5% ложных, 1 из 3 при 3.19 — в пределах ожидаемого макс.). ±-симм/маргиналы
  вычеркнуты (зеркало, тождественны).
- **ИТОГ: R3-geo зелёный по существу (приёмка+физика+зеркало); гейт R4 ОТКРЫТ.** atan2-Коссера
  остаётся deprecated. cosserat_geo — рабочая анизотропная энергия. Коммит 4223e20.

### R4a — ЗЕЛЁНЫЙ + аудит немонотонности (2026-07-07)

R4a (сектор Tw=0, cosserat_geo+spinor, зеркальные пары, k_t/k_b∈{0.1,1}, 1407с).
Контроли чистые: зеркало 1.27/0.16<3.08 ✅, воспроизв. 3.05/2.70 ✅, связь Tw=0 сохранена
max|ΔTw|=6.3e-5/2e-9 ✅, сходимость 92%/100%. **Центральный вопрос: антиферро в M1/M2 — НЕТ**
(обе ферро, h=+1 всюду, M2≡M1, кинки=0: спинор при T=0 разглаживает). Условное E(θ|h) н/о.
ЛОЖНАЯ строка авто-вердикта «зависимость от сектора Tw=0.683» — артефакт (1 сектор, старый код
до фикса; фикс в коде для R4b). Аннотирована.

**Эффект (НЕ интерпретировать до аудита, прецедент R1-транзиента):** связь Tw=0 у изгиб-жёсткой
ячейки подавила E(0) 0.999(свободно R3geo)→0.752 и создала НЕМОНОТОННУЮ кривую E(30)=0.987>E(0)=0.752.
У изотропной — мелко (0.377→0.359).

**АУДИТ (архитектор, до R4b):** (1) точечная сходимость изгиб-жёсткой θ∈{0,15,30}, потолок
600k, тренд E по блокам: E(0)→~1 ⇒ транзиент, хороним; стабилен ⇒ реальный constrained trapping.
(2) энергоаудит по ветвям на финалах θ=0: остаточная E изгиб/твист минор vs мажор; профиль n_i·a
(гипотеза π-дуга) и τ̃_i (компенсирующая скрутка). Итог — results/R4a/audit.md.
R4b после аудита (независимо): + немонотонность (E(30)−E(0)) по секторам.

**ИТОГ АУДИТА (results/R4a/audit.md):** немонотонность = ТРАНЗИЕНТ, ХОРОНИТСЯ (пре-рег i).
Точечная сходимость до 600k: E(0) ползёт −0.126→+1.000 (к 400k), |E30−E0| спадает 0.335→0.005.
При сходимости изгиб-жёсткая Tw=0 → ферро-СТУПЕНЬ E(0)≈E(15)≈E(30)≈+1.0, монотонная. R4a-потолок
200k поймал траекторию на подъёме (E≈0) ⇒ «подавление E(0)=0.752» и пик — артефакт неполной
релаксации. Энергоаудит θ=0: 100% мажор, минорной ветви НЕТ ⇒ π-дугу проверить нельзя (её нет в
равновесии). ВЫВОД: связь Tw=0 НЕ меняет осевое равновесие (та же ступень, что R3geo без связи) —
согласуется со спинорным no-go (осевые наблюдаемые гейдж-слепы к твист-топологии). (Авто-вердикт
скрипта имел знаковый баг в пороге немонотонности — исправлен; данные однозначны.)
СЛЕДСТВИЕ для R4b: изгиб-жёсткая ячейка требует потолок ≥400k (иначе транзиент); поднять ceiling.

### R4b — КРАСНЫЙ КОНТРОЛЬ (стоп): сектор Tw=2π фрустрирован/бистабилен

R4b (сектора Tw=0 vs 2π × k_t/k_b∈{0.1,1}, cosserat_geo+spinor, зеркальные, ceiling 600k, 7159с).
Авто-вердикт «E зависит от сектора Tw ⇒ топология видна» (max_θ|ΔE|=1.95/0.69) — НЕДОСТОВЕРЕН.

**КРАСНЫЙ КОНТРОЛЬ — воспроизводимость сектора Tw=2π провалена:**
- Tw=2π k_t/k_b=0.1: **508σ ❌** — БИСТАБИЛЬНОСТЬ при θ=0: сид0 E=+0.99, сид1 E=−0.81
  (противоположные ветви!). Локированная 2π-скрутка в изгиб-жёсткой ленте создаёт конкурирующие
  минимумы; разные сиды → разные бассейны.
- Tw=2π k_t/k_b=1: **14.83σ ❌** (E@0=+1.0 сдвинут vs Tw=0=0.36, но сиды рассогласованы).
- Сектор Tw=0 ЧИСТ: воспроизв. 1.99/2.70 ✅, зеркало все ✅. Сходимость Tw=2π 70/80% (Tw=0 100%).

**Природа:** сектор Tw=2π ФРУСТРИРОВАН — локированный 720°-твист конкурирует с зажимами,
рождая множественные метастабильные минимумы (бассейн-зависимость от сида). Знак ферро везде,
антиферро нет, кинки=0 (спинор T=0). M2≡M1.

**Интрига (НЕ интерпретирую):** Tw=2π ДЕЙСТВИТЕЛЬНО меняет E(θ) vs Tw=0 (топология, возможно,
видна!) — НО через фрустрацию, а не чистую меру. Вопрос архитектору: (i) фрустрация сама и есть
сигнал топологии, или (ii) нужен отжиг/иная мера для разрешения бистабильности? Бистабильная
изгиб-жёсткая ячейка не даёт определённой E(θ) — измерять сектор-зависимость на ней нельзя.

**СТОП по протоколу (красный контроль). Мягкая лента НЕ запущена.** Пре-регистрация R4b не тронута.
R4a (Tw=0) остаётся зелёным. Гейт R4b→мягкая закрыт до решения архитектора о фрустрации Tw=2π.

### R4b v2 (честный сэмплинг) — фикс сработал, но Tw=2π РЕАЛЬНО бистабилен (красный, стоп)

Фикс degenerate-init (sector_sample) перегнан (7096с, ceiling 600k). Результат разделил артефакт и физику:
- **Tw=0 сектор ПОЧИНЕН:** сиды СОГЛАСНЫ (доли ветвей совпадают), max|ΔE|≤0.05 ✅. Прежние
  сид-флипы Tw=0 ушли. (Метрика воспроизв. 7.37σ для Tw=0 kt/kb=0.1 — АРТЕФАКТ насыщения:
  при E→±1 σ→0, взрывает z; абсолютное max|ΔE|=0.05 мало ⇒ воспроизводимо. Как R3-geo kt/kb=10.)
- **Tw=2π сектор — РЕАЛЬНАЯ БИСТАБИЛЬНОСТЬ, переживает разнообразную инициализацию:** kt/kb=0.1
  max|ΔE|=1.94, на ИЗОЛИРОВАННЫХ θ (30°,120°,172.5°) сиды в ПРОТИВОПОЛОЖНЫХ ветвях (θ=172:
  +0.95 vs −0.98). Не гладкий сдвиг недосходимости, а 3/25 точек с конкурирующими минимумами.
  kt/kb=1 max|ΔE|=0.47. Честный сэмплер УБРАЛ артефакт и ОБНАЖИЛ: залоченный 2π-твист реально
  фрустрирует изгиб-жёсткую ленту при определённых θ (два почти-вырожденных глобальных минимума).

**Диагностика (архитектор) отработала: (1) вырождение было, (2) фикс убрал его для Tw=0,
(3) остаточный красный Tw=2π — ФИЗИКА (топологическая фрустрация), не артефакт.**

Пре-рег: (a) антиферро-ветвь в Tw=2π существует (>0) ✅; (c) сектора различаются ✅ — НО Tw=2π
не воспроизводим ⇒ «подтверждённым каналом видимости топологии» назвать НЕЛЬЗЯ (§4.2 красный).

**СТОП по протоколу (красный контроль Tw=2π). Отжиг (шаг 5) и мягкая лента НЕ запущены** —
гейт был «после ЗЕЛЁНОГО (3)», а (3) красный (но по физической, не артефактной причине).
Вопрос архитектору: бистабильность Tw=2π = САМ сигнал топологии (докладывать как есть, фрустрация
= видимость), ИЛИ запускать R4b-anneal (шаг 5) чтобы отжиг выбрал меру и разрешил бистабильность
в определённую E(θ|2π)? Обе трактовки легальны. Tw=0 остаётся зелёным. Пре-рег R4b не тронута.

### R4b — РЕКЛАССИФИКАЦИЯ + R4b-anneal (2026-07-08)

Решение архитектора по красному Tw=2π:
1. **Реклассификация:** красный Tw=2π — НЕ дефект воспроизводимости, а ВЫРОЖДЕННОСТЬ основного
   состояния при T=0 (E не самоусредняется в точках фрустрации). Контроль §4.2 в таких точках →
   вердикт **"DEGENERATE"** (сид-флип при ЧИСТОМ зеркальном контроле И честном сэмплере) — валидный
   физический исход, НЕ стоп-сигнал.
2. **Результат (i) ДОСТИГНУТ и зафиксирован:** топология видна на уровне СТРУКТУРЫ ЛАНДШАФТА —
   фрустрация и антиферро-ветвь существуют ТОЛЬКО в Tw=2π (в Tw=0 их нет). Главный вывод R4b.
3. **R4b-anneal (обязателен, снятие вырожденности):** обе Tw=2π ячейки + Tw=0 контроль, T0=1,
   decay∈{0.9995,0.9999}, честный сэмплер, зеркальные пары, 2 сида. Наблюдаемые: E(θ|сектор),
   доли ветвей P(st|θ,сектор), НОВОЕ — профиль весов конкурирующих ветвей в бывших точках
   фрустрации (θ=30,120,172.5 + соседние). Пре-рег: (a) отжиг устраняет сид-флипы (веса
   воспроизводимы) — если нет, вырожденность точная (симметрия?), отдельная загадка; (b) доля
   антиферро в Tw=2π θ-зависима, форма открыта; (c) КЛЮЧЕВОЕ — два decay дают согласованные
   веса (термо-мера определена, не зависит от скорости охлаждения); если веса зависят от decay —
   мера неуниверсальна.
4. После зелёного/осмысленного R4b-anneal — мягкая лента.

### R4b-anneal — ИНКОНКЛЮЗИВНО (двойной конфаунд, аудит) (2026-07-08)

R4b-anneal (8 ячеек: decay{0.9995,0.9999}×Tw{0,2π}×kt/kb{0.1,1}, T0=1, честный сэмплер,
зеркальные, 15550с). Авто-вердикт (a)❌«вырожденность точная» и (c)⚠️«мера неуниверсальна
max|Δвес|=0.484» — НЕ приняты (дисциплина аудита, прецеденты R1/R4a):
1. **Недосходимость Tw=2π:** Tw=0 сошлись 100%, Tw=2π только 46–80% (потолок 600k; d0.9999
   Tw2π kt/kb0.1 — 46%, внутрисидовое max|E0−E1|=1.94). Сид-флипы/decay-разброс = недо-равновесие.
2. **Конфаунд амплитуды отжига:** «медленнее→выше амплитуда» (R2-лайт) сдвигает веса даже в
   СОШЕДШЕМСЯ Tw=0 kt/kb=1 (амп 0.642→0.989 по decay). (c) в текущем виде мерит и это.
ЧИСТОЕ: (α) Tw=0 контроль сходится 100%, воспроизводим (max|E0−E1|≤0.02); (β) сектор Tw=2π
ГЛАССИ — не термализуется за 600k (фрустрация→медленная динамика, самостоятельная сигнатура);
(γ) антиферро-ветвь в Tw=2π существует (>0). (a)/(c) ИНКОНКЛЮЗИВНО. Термо-мера фрустрированного
сектора не достижима этим протоколом.
ВОПРОС АРХИТЕКТОРУ: (i) сильно поднять потолок (дорого, глассность может не уйти); (ii)
контролировать конфаунд амплитуды (сравнивать при равной насыщенности/ещё медленнее до сатурации
обоих); (iii) принять глассность Tw=2π как вывод (фрустрация→неэргодичность); (iv) иное.
Мягкая лента НЕ запущена (гейт «после осмысленного R4b-anneal»; результат инконклюзивен).
Зелёные: A,B,R2-лайт,R3-geo,R4a,R4b(Tw=0-контроль).

### R4b-PT — параллельное темперирование (вариант iv, решение архитектора, 2026-07-08)

Штатный сэмплер для глассовых ландшафтов. (i) потолок отклонён (экспон. барьеры), (ii)
конфаунд амплитуды поглощён дизайном PT, (iii) — фолбэк если PT не эквилибрирует в таймбокс.

R4b-PT дизайн:
1. R=10 реплик/ленту, геом. лестница T от T_hot (калибровать: acceptance у горячего конца ≥40%)
   до T_cold=1e-3; обмен соседних реплик каждые 100 шагов по Метрополису на Δ=(β_i−β_j)(E_i−E_j);
   vmap (лента×реплика), B=2048. Тесты: детальный баланс на игрушечной двухъямке (больцмановский
   вес восстанавливается); сохранение Tw при обменах (сектор общий для реплик ленты).
2. Точки: фрустрированные θ∈{30,120,172.5}+соседи{22.5,37.5,112.5,127.5,165,180}+контроль{0,90};
   k_t/k_b=0.1, сектора Tw=0 и Tw=2π (НЕ весь свип).
3. Критерии эквилибрации (ВСЕ обязательны): acceptance 20–40% на всех звеньях; ≥3 round-trip
   реплик по лестнице; согласие весов ветвей на T_cold между 2 сидами И 2 лестницами (R=10 vs 14) в 2σ.
4. Пре-рег: (a') PT эквилибрирует 2π (крит.3) — если нет за 2× стоимости R4b-anneal, фиксируем (iii)
   неэргодичность как сигнатуру; (b') веса P(st|θ,2π) на T_cold воспроизводимы И не зависят от
   лестницы — мера определена; (c') форма θ-зависимости антиферро — открыто, СВЕРКА С cos² ЗАПРЕЩЕНА
   до фиксации сырых весов в отчёте (защита от подгонки взгляда).
5. Отчёт: веса ветвей по θ/секторам, диагностика PT (acceptance, round-trips), вердикты (a')-(c').
   Мягкая лента — после.

### R4b-PT — РЕЗУЛЬТАТ: топология переворачивает знак корреляции (аудит, 2026-07-09)

PT прогнан (348 мин, в таймбоксе). Реализация валидирована (5 тестов: двухъямка/больцман/Tw/свопы/
диагностика). Калибровка: R=10/14 не мостят энергошкалу k_b≈90 (hot-end acc 0.12); R=30 (min-acc
0.39, T_hot=1.5). Сетка: 2 сектора × {R30 2сида, R40 сид0} × θ{0,30,90,120,172.5}, B=1024, 2000 бл.

**СЫРЫЕ ВЕСА (доля антиферро s·t=−1, зафиксированы, cos²-сверка запрещена):**
- Tw=0: θ{0,30,90,120,172.5}=0.00/0.00/0.48/1.00/1.00 (сиды идентичны) — чистый ФЕРРО, E(0)=+1.
- Tw=2π: 0.87/0.86/0.48/0.26/0.13 (сиды 0.012-0.034) — АНТИФЕРРО-наклон, E(0)≈−0.74.

**ГЛАВНОЕ: топологический сектор Tw=2π ВОСПРОИЗВОДИМО ПЕРЕВОРАЧИВАЕТ корреляцию к антиферро** —
противоположный знак Tw=0. Топология видна НА УРОВНЕ МЕРЫ (не только структуры ландшафта, как в квенче).

**АУДИТ авто-вердикта (iii слишком пессимистичен):** (1) round-trips=0 и для КОНТРОЛЯ Tw=0 (чистого,
сид-идентичного) ⇒ это ограничение длинной лестницы, НЕ неэргодичность 2π; (2) сэмплинг СИД-
ВОСПРОИЗВОДИМ (seed_gap=0.034 ✅); (3) единственный провал — маргинальная ладдер-зависимость 0.057
(чуть >0.05, R30 vs R40 ~6%). Точная мера не до конца сошлась, КАЧЕСТВЕННО устойчива.
Пре-рег: (a') строго НЕ пройдено (round-trips/ладдер), но НЕ (iii); (b') сид-воспроизводимо ✅,
ладдер маргинально ❌ (0.057); (c') сырые веса зафиксированы, форма антиферро θ-зависима.

ВОПРОС АРХИТЕКТОРУ: (α) принять качественный сигнал (топология переворачивает знак, сид-воспроизв.)
как результат (b')/(c'); (β) довести сходимость лестницы (больше блоков/ступеней) для точной меры
и снятия 0.057; (γ) теперь можно ли сверять форму Tw=2π с cos²/семействами (c' был заблокирован).
Мягкая лента — после решения. Зелёные: A,B,R2-лайт,R3-geo,R4a,R4b(Tw=0),PT-инфраструктура.

### R4b-PT принят (α) + R4c дожатие меры (решение архитектора, 2026-07-09)

(α) ПРИНЯТ — ГЛАВНЫЙ РЕЗУЛЬТАТ ФАЗЫ C: знак корреляции НАСЛЕДУЕТСЯ от топологического сектора
(Tw=0 ферро / Tw=2π антиферро), видимость на уровне меры. Онтология: 2π = нетривиальный ℤ₂-класс.
(γ) сверка разблокирована. Первичная сверка архитектора (в отчёт R4c): знак и нуль на 90°
совпадают с синглетом, края задавлены (E(0)=−0.73 вместо −1) — «ножницы» на уровне меры; на
семействе хорды кривая НЕ лежит (E(0)≠−1).

R4c — дожатие меры (таймбокс 1.5× R4b-PT ≈ 8.7ч):
1. Лестница АДАПТИВНАЯ (равный acceptance, не геом.), свопы каждые 10 шагов (было 50-100), блоки
   длиннее; цель round-trips≥3 и ладдер-гэп <2σ (две независимые лестницы).
2. Полный свип θ (25 точек), оба сектора, k_t/k_b=0.1, 2 сида.
3. Пре-рег: (a) КЛЮЧЕВОЕ — P_анти(θ=0|2π)→1 при дожатой эквилибрации ИЛИ выходит на пол <1
   (ферро-примесь термодинамическая)? обе легальны, уверенность низкая; (b) веса ладдер- и
   сид-независимы (<2σ) — мера определена; (c) фиты формы ПОСЛЕ фиксации весов: масштаб. синглет
   −A·cosθ, хорда E_p, ступень/пила; гармоники A1/A3; MLE+AIC, bootstrap CI.
4. Диагностика меры (гистограммы c_A,c_B по ветвям) на T_cold — впервые для антиферро-сектора;
   сверка с КС-эталоном ∝ max(c,0).
5. Мягкая лента — после R4c (её h-вопрос частично поглощён: PT-горячие реплики населяют кинки
   при сохранённом Tw).

### R4c принят (кульминация C) + R4d темп-развязка ножниц (решение архитектора, 2026-07-09)

R4c ПРИНЯТ как кульминация фазы C: топологический заряд задаёт знак И синглетную форму корреляции
через ТЕРМО-МЕРУ бассейнов. ЧЕСТНАЯ РАМКА (в отчёт): CHSH=A·2√2≈1.33<2 — кривая ВНУТРИ классического
предела; квантовость требует A=1, «ножницы» переехали на уровень меры (пол ферро-примеси 0.26
термодинамический). (b)-строгость поглощается R4d (больше B на ключевых точках), не отдельно.
Мягкая лента — после R4d.

R4d — температурная развязка ножниц (РЕШАЮЩИЙ):
0. БЕСПЛАТНО до прогона: из весов R4c извлечь ΔF(θ)=T_cold·ln[(1−P)/P], фит линейности по cosθ;
   сверка E(θ) с −tanh(ΔF/2T). В отчёт.
1. Свип T_cold∈{3e-3,1e-3,3e-4,1e-4} (лестница переадаптируется на каждый T), Tw=2π, k_t/k_b=0.1,
   θ-сетка 15°, B=1024+ на ключевых (0,90,180), 2 сида, критерии PT из R4c (RT≥3, ладдер-независ.).
2. Пре-рег (гипотеза архитектора tanh/линейный отклик): (a) амплитуда A(T) растёт с ↓T_cold;
   (b) форма уходит от косинуса — |A3/A1| растёт (уплощение к СТУПЕНИ); (c) E(θ,T) коллапс на
   мастер-кривую −tanh(c(θ)·β), ΔF не зависит от T (масштабирование).
   ФАЛЬСИФИКАТОР: A→1 при A3≈0 (чистый косинус до амплитуды 1) — гипотеза мертва, СТОП+эскалация
   (кандидат на квантовую меру!).
3. Диагностика меры (c_A,c_B по ветвям, КС-сверка) для антиферро на самом холодном T — в отчёт.

### R4d РЕЗУЛЬТАТ (2026-07-09): термо-мера = масштаб. синглет, амплитуда растёт глациально

R4d прогнан (326 мин). Свип T_cold∈{3e-3,1e-3,3e-4,1e-4}, mean-RT 7-12 ✅. Точная картина
(tanh-модель совпала количественно): E(θ|2π)=−tanh(c(T)·cosθ/2), c=|ΔF|/T.
- ΔF≈−1.1·T·cosθ (наклон ΔF/T стаб. ~−1.1, слабый интринсик-рост -0.96→-1.13/декаду; R²(cosθ)≥0.988).
- Режим МАЛОГО c (~1): tanh почти линеен ⇒ форма КОСИНУСНАЯ (A3/A1~0.04≈0). (b) «уплощение к ступени»
  НЕ наблюдается (ступень лишь при c≫1, нужно c~3 для A=1 = ~10 декад холода). Авто-(b)✅ некорректен.
- (a) A растёт ГЛАЦИАЛЬНО с замедлением: 0.29→0.46→0.49→0.52. Квантовый A=1 практически недостижим.
- Фальсификатор (A→1 при A3≈0) НЕ сработал: A3≈0 ✅ (косинус), но A=0.52≪1 растёт ледниково.
ВЫВОД: топ.сектор Tw=2π классической ленты = МАСШТАБ. СИНГЛЕТ −A·cosθ, A≈0.5 растёт к 1 лог-медленно
(термо-ножницы = устойчивая термодинамич. черта). Борн (A=1, CHSH=2√2) — формальный T→0-предел,
недостижимый. CHSH≈1.48<2 (классика). Топология даёт ЗНАК+синглетную ФОРМУ; квантовую АМПЛИТУДУ
классическая мера НЕ даёт. Открытая задача note §3: ОТРИЦАТЕЛЬНО для квантовости, ПОЛОЖИТЕЛЬНО для
механизма знака/формы. Коммит после. Мягкая лента — следующая (последний пункт очереди).

### R4d принят — мера ЭНТРОПИЙНАЯ + R4e экстенсивность + мягкая-lean (архитектор, 2026-07-09)

R4d принят. КЛЮЧЕВОЕ дополнение (в отчёт): ΔF∝T ⇒ ΔE≈0, мера ЭНТРОПИЙНАЯ (ΔS≈1.1·cosθ) —
вероятность ветви = ПОДСЧЁТ МИКРОКОНФИГУРАЦИЙ. Программа §6 РЕАЛИЗОВАНА с синглетной формой; предел
A=tanh(ΔS/2). Борну нужны ТОЧНЫЕ НУЛИ (запрет исхода) — энтропийный счёт их НЕ даёт.
ЖУРНАЛ: гипотеза (b) «энергетический tanh» ОПРОВЕРГНУТА, механизм ЭНТРОПИЙНЫЙ. Вторая исправленная
гипотеза архитектора за проект (первая — «p≈2 транзиент R1»... нет, первая была float32-диагноз R3).

R4e — экстенсивность ΔS (дёшево, решающее для леммы):
1. N∈{32,64,128}, θ∈{0,60,90}, T_cold=3e-4, сектор 2π (заряд ТОТ ЖЕ 2π, не масштаб.), k_t/k_b=0.1,
   PT-протокол R4c. Извлечь ΔS(N)=ln[P/(1−P)] из ΔF/T.
2. Пре-рег: (а) ΔS∝N → амплитуда управляема, но ножницы по N (форма уйдёт от cos при больших N);
   (б) ΔS≈const(N) → амплитуда ТОПОЛОГИЧЕСКИ ЗАПЕРТА на tanh(ΔS/2). Обе легальны.

Мягкая-lean (закрытие полноты, последний непротестир. режим): одна ячейка k_e=1≈T0, spinor,
отжиг 0.9995, диагностики h-популяций, E(θ|h), M2, ВРЕМЕННЫЕ РЯДЫ M1/M2 на θ∈{45,90}.

После R4e+мягкой — фаза C закрывается; сводный отчёт и note §3 пишет архитектор.

**R4e РЕЗУЛЬТАТ (2026-07-10): ИНКОНКЛЮЗИВНО — ΔS(N) scatter+смена знака.** ΔS(0°)=−3.15(N=32 ферро)/
+1.14(N=64 антиферро)/−0.09(N=128 ~ноль). Линейный фит разброс 615% ⇒ авто-«∝N» НЕДОСТОВЕРЕН. N=64
согласован с R4c/R4d (солиден); N=32/128 аномальны, сходимость НЕ подтверждена (аудит по сидам упёрся
в таймауты, N=128 медленный R=27; mean-RT 5.7-6.3 адекватны, но смена знака подозрительна — прецеденты
R1/R4a). Лемма §3 экстенсивности НЕ решена. Нужен выделенный аудит N=32/128 (длиннее+сиды). Tw сохранён
3.3e-6. Не заявляю (а)/(б). Мягкая-lean запускается параллельно/следом.

### Фаза C ЗАКРЫТА + R4e-аудит (последний прогон, решение архитектора, 2026-07-10)

Сводный отчёт архитектора: results/phase_C_report.md; note §3 обновлена резолюцией. Мягкая лента
принята, отрицательное множество полно.

R4e-аудит (экстенсивность ΔS, финал):
1. Диагноз scatter/смены знака N=32/128: лестница PT НЕ масштабировалась с N (полная энергия ∝N,
   T_hot — нет) → недоэквилибрация. Плюс МОЙ БАГ: k_b∝N (κ=1-нормировка) вместо пер-линк-константы.
   Фикс: (i) пер-линк k_b=90,k_t=9 КОНСТАНТНЫ (материал один, меняется длина); (ii) на каждый N
   лестница калибруется заново — T_hot из прогрева до acceptance≥40% на горячем звене + адапт. шаг.
2. N∈{32,64,96,128}, θ∈{0,60,90}, T_cold=3e-4, Tw=2π фикс, 2 сида, критерии R4c (RT≥3,
   ладдер-независимость, DEGENERATE для вырожденных).
3. ΔS(N); фит const vs линейный по N (AIC + bootstrap CI наклона). Пре-рег: (а) ΔS∝N → контроль
   N=256 θ=60 (форма уходит от cos = ножницы по N); (б) ΔS≈const → амплитуда топологически заперта,
   лемма форма (i).
4. Финальная строка в лемму-кандидат, коммит phase-C-final. Фаза C закрыта полностью.

**R4e-АУДИТ РЕЗУЛЬТАТ (2026-07-13): лемма НЕ решена.** Фиксы (пер-линк k_b=90 конст, лестница под N)
улучшили эквилибрацию (RT 5-10, сид-согласие 0.03-0.17), НО ΔS(N) остался немонотонным со сменой
знака: −1.98(N=32 ферро)/+1.20(N=64 антиферро)/−0.02(N=96 ~0)/+0.03(N=128 ~0). Оба фита катастрофичны
(const χ²=1378, лин χ²=1262 для 4 точек) ⇒ авто-«экстенсивна» НЕВАЛИДЕН (наклон = МНК-артефакт через
разброс). N=64 антиферро рок-солид (R4c/d/b-PT); N=32 ферро аномалия (сид 0.17 маргин.); N≥96 ΔS≈0 —
дилемма finite-size-затухание vs недоперемешан (RT падает 9.7→5.2), проверка N=96 2×блоков упёрлась в
таймауты. ЛЕММА §3 экстенсивности НЕ РЕШЕНА (ни (а)∝N ни (б)const). Главный результат (топология→
знак+синглет через энтроп.меру) валиден при N=64, но N-скейлинг messy (возможно finite-size). Нужен
ВЫДЕЛЕННЫЙ прогон N≥96 большой N_BLOCKS — решение архитектору. phase-C-final коммит с этой оговоркой.

### R4e-v2 — коммензурабельность укладки заряда (решение архитектора, 2026-07-13)

Дихотомия (а)/(б) пре-рег признана ЛОЖНОЙ: таблица знаков ΔS подсказывает ТРЕТИЙ сценарий —
КОММЕНЗУРАБЕЛЬНОСТЬ/ЧЁТНОСТЬ укладки заряда 2π на дискретную цепь (твист/линк = 2π/(N−1)).

R4e-v2 (дёшево, вместо N≥96×4блоков):
1. Малые N: N−1∈{16,24,32,40,48,56,64} + пары чётности (N,N+1) для трёх точек. θ∈{0,90}, T_cold=3e-4,
   критерии R4c. Малые N эквилибрируются надёжно (RT≈10) — данные чистые.
2. Пре-рег заново: (α) знак ΔS следует арифметике N−1 (чёт/нечет или mod-класс) — коммензурабельный;
   N≥96-нули = гашение укладок; (β) внутри класса чётности ΔS стабилизируется → континуальный предел
   в классе (R4c/d жив с оговоркой дискретизации); ΔS→0 → finite-size, оговорка обязательна;
   (γ) знак не следует арифметике → выделенный длинный прогон.
3. Профиль ⟨τ̃_i⟩ по ветвям для двух N разной чётности — геом. портрет укладки (закрывает вопрос
   локализован/размазан заряд).
4. Финальная редакция леммы §3, настоящий phase-C-final-v2.

- **R4 (Tw=const, после зелёного R3):** проекция градиента g←g−(g·∇Tw/|∇Tw|²)∇Tw,
  Tw=Στ_i. Тест |ΔTw|<1e-4. R4a: сектор Tw=0, спинор, свип θ, M1/M2 (антиферро?
  центральный вопрос, обе гипотезы легальны). R4b: Tw=0 vs Tw=2π бок о бок (ℤ₂), зависит
  ли E(θ) от сектора. Мягкая лента: спинор, k_e∈{1,3}≈T0, decay=0.9995, h-популяции,
  E(θ|h), M2 — P(h=−1)>0 впервые, расщепление E по h открыто.
