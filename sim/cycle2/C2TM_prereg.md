# C2-TM prereg (утверждён Артемом 2026-07-16). Замкнутая форма A(k_f).
Данные этапа 1: ЗАМОРОЖЕННОЕ сырьё S1runs (a9cef7b), 4 точки. Формы-
кандидаты (фиксированы здесь, ≤2 свободных параметров, добавление форм
после взгляда на новые данные ЗАПРЕЩЕНО):
  M1: A = tanh(c·k_f^γ);  M2: A = k_f^γ/(k_f^γ + k0);
  M3: A = 1 − exp(−c·k_f^γ);
  M4 ВЫЧЕРКНУТА (Q7): вывод трансфер-матричной формы архитектором
  к prereg-коммиту не представлен. Семейство кандидатов: M1-M3,
  закрыто, добавления запрещены.
Этап 1 (без прогонов): фит M1–M3 на 4 точках, AICc; ПРЕДСКАЗАНИЯ каждой
формы при k_f∈{0.25, 8} — в этот файл, коммит ДО прогона новых точек.
Прогноз архитектора (в журнал): A(8)=0.95±0.03, A(0.25)=0.16±0.04.
Этап 2 (после утверждения, GPU): точки k_f∈{0.25, 8} (+ по бюджету 6.0),
N=32, M=1200, T=0.05, протокол S1-runs, сырьё→коммит→анализ.
KILL: ни одна форма из списка не даёт χ²/dof<2 на объединённых ≥6 точках
С попаданием held-out предсказания в 3σ ⇒ простой трансфер-формы нет,
A(k_f) остаётся кинетическим числом без замкнутой формы.
