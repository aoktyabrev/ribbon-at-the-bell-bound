# C3-B-mech addendum 1 (архитектор): T_prep + перекласс M0. Коммит ДО M0-full.
# Разрешает ARCH-QUESTION C3Bmech_M0_STOP_note (дизайн-пробел конечно-T).

## A-T1 — T_prep зарегистрирована
**T_prep = 0.5** (масштаб T_hot/4 отжиговых кампаний C2; выше стекольных
барьеров ~0.1-0.2 из C2-J смоука, ниже T_hot=2.0 полного расплава).
Протокол M0:
1. отжиг до ПЛАТО при T_prep, поле h активно; эргодичность = дрейф ⟨c⟩ <σ
   за последние 20% шагов;
2. поле h ВЫКЛЮЧАЕТСЯ, квенч T_prep→0 БЕЗ поля (заморозка ориентационного
   беспорядка — снятие намагничивающего поля до опыта, рифма A1);
3. измерение μ на ЗАМОРОЖЕННОМ ансамбле.

## A-T2 — канонический ансамбль
Канонический M0 = ТЕПЛОВОЕ ПЛАТО при T_prep (для формы 1+χn_z каноничен
Больцман). Квенч-ветка (после снятия h) — часть протокола (A-T1 шаг 2);
её сохранность биаса — отдельная строка отчёта: **χ_frozen/χ_plateau**
(прогноз: биас переживает квенч 0.7-1.0 — 0.6).

## A-T3 — гейт A3 переопределён
h_max по rej/step<1e-3 НА ПЛАТО T_prep + валидность фита формы 1+χn_z
(χ²/dof<2) на плато.

## A-T4 — ПЕРЕКЛАСС M0 = КАЛИБРОВКА (не гипотеза)
M0 меряет масштаб χ(h, T_prep) — калибровочная стадия, НЕ тест.
**Прогнозы M0 0.6/0.55 ОТЗЫВАЮТСЯ из счёта** (регистрация ошибочно придала
калибровке статус теста — ошибка дизайна архитектора, в журнал).
Содержательные гипотезы фронта A (в счёт):
- **M1** (D_mech>0 на механич. ансамблях против аналитики) — **0.5**;
- **M2** (нуль-множество {2} на механич. ансамблях) — **0.4**;
- сохранность биаса в квенче (A-T2) — **0.6**.
**KILL M0 остаётся ТОЛЬКО технический:** форма 1+χn_z не фитится (χ²/dof≥2)
⇒ мера не of the form ⇒ аналитика (C3Bmech) неприменима ⇒ стоп-дизайн.

## Порядок: этот addendum коммитом → M0-full (плато T_prep, сетка h до h_max,
## оба χ: плато и frozen, фит формы) → отчёт → M1/M2 после go.
