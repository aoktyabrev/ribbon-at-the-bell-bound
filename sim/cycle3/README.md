# Цикл 3 — «Причинность и асимметрия». Статус: этап 0, черновики.

Ставки:
- **C3-B** (несимметричная лента): закрыть ИМПОРТ #2 — вывести
  партиально-запутанную рулёжку ансамблей ВНУТРЕННЕ (из асимметричного
  закона ленты P(s,t|a,b,χ)), чтобы no-signaling форсировал p=2 БЕЗ
  импорта КМ-ансамблей. Теорема «Борн из причинности» — внутренне.
  Канон §3.2 признаёт импорт: «within this ansatz and its stated
  partially entangled extension… we make no claim of a general
  derivation of the Born rule» (FINAL_v1:284-290).
- **C3-L** (фолиация): позитив-предложение структуры фолиации +
  no-go-кандидат по дыре #5.
- **C3-G** (основное состояние жёсткой цепи): стена структурной
  декорреляции цикла 2 (C2-J квенч-стекло — A~0.21 при коллапсе E_std
  ~100×, декорреляция near-ground-state). Структура основного состояния
  фрустрированной жёсткой цепи — открытая задача с цикла 2.

Порядок этапа 0: сверка первоисточников → scaffold → драфты (C3-B первый).

## Методология (без изменений + упрочнения цикла 2)
- prereg-коммит ДО данных; kill-критерии — Артем (владелец); сырьё→
  коммит→анализ; зеркала; изотропная CHSH-формула запрещена; S>2 =
  стоп-аудит.
- **Упрочнение цикла 2:** чек-лист prereg↔JSON — ИСПОЛНЯЕМЫМ assert'ом
  ДО старта прогона (4 инстр.-пропуска цикла 2: F_t, R, проекции, F_s —
  все бэк-филлом; впредь assert падает до сырья).
- прогнозы называют эстиматор (ρ_tri vs ρ_cos не взаимозаменяемы);
  числа архитектора существуют ТОЛЬКО внутри кодблоков; бюджет
  кампании перемножается при регистрации, не при запуске.
- метрика = наблюдаемая (урок J0-v2/dt-арбитраж): сходимость сверять
  в ОДНОЙ метрике модели.

## Приоры архитектора (журнал)
C3-B: семейство, проходящее B1-B3,B6,B7, существует — 0.55 (фаворит
F2); D(χ)>0 на промежуточных χ у прошедшего семейства — 0.5;
при D>0: единственность p=2 по всем χ — 0.75; полная внутренняя
теорема к концу цикла — 0.4; стена «B5/D≡0 всеми семействами» — 0.3.
C3-L: пара (позитив-предложение о фолиации + no-go-кандидат) — 0.5.
C3-G: доменная картина объясняет декорреляцию — 0.5.

Снято/закрыто цикл 2: C2-F/TM/ISO/A/L/R/J (см. cycle2/C2_synthesis.md).
Причинная структура: §2.6(ii) оговорка → C2-F измерение (F_s<6.3e-4).
