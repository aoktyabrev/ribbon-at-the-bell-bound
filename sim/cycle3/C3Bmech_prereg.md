# C3-B-mech prereg FINAL (A1-A7 вложены) — механич. реализация χ. Коммит ДО батарей.
# Черновик: C3Bmech_prereg_DRAFT.md; аналитика премисы: C3Bmech_analytics.md
# (D_mech>0 ГИВЕН поляризацию; стена конусного срезания ОПРОВЕРГНУТА — из счёта снята).

## Цель
Породить μ_χ ∝ 1+χ n·ẑ из УПРУГОЙ динамики (band4d/measurement, циклы 1-2),
не постулируя. Make-or-break = M0 (поляризуется ли источник полем h).

## Фиксации A1-A7 (дословный смысл в prereg):
- **A1 — поле h только PREP:** аддитивный член −h·Σ_i(n_i·ẑ) (намагничивание
  источника) активен в prep/отжиге, ВЫКЛЮЧЕН до измерительной релаксации.
  Ядро энергии модели НЕ меняется; SPEC += приложение «prep-протокол с
  поляризующим полем», пометка **ARCH**. Измерит. динамика каноническая
  (сравнимость с циклами 1-2 сохранена).
- **A2 — n:** осевая наблюдаемая n_end=rotmat(u)·ê (эстиматор циклов 1-3,
  без изменений). Источник: ПЕРВИЧНАЯ = ось среднего сегмента n_mid
  (усреднение реперов центральной трети — меньше зажимного загрязнения);
  вторичная = концевые n_A,n_B. Зафиксировано до прогонов.
- **A3 — χ_max^phys:** по гейту стабильности h_max = наибольшее h с
  rej/step<1e-3 И валидным фитом 1+χn_z (χ²/dof<2). Предел позитивности
  χ≤1 — матем. потолок, не физ. критерий.
- **A4+A5 — оба режима:** M0 в квенче И отжиге (slow×16). Правило выбора
  ансамбля для M1/M2 (регистр. до прогонов): ПЕРВИЧНЫЙ = отжиг при проходе
  M0; ФОЛЛБЭК = квенч.
- **A6 — при стене M0:** клейм статьи 3 НЕ меняется (теорема отбора о классе
  МЕР, премиса феноменологична и заявлена таковой); стена = отдельный
  результат «упругий источник не поляризуется [механизм]» в Outlook-долги.
- **A7 — порядок:** M0 сразу, параллельно γ-ревью фронта B.

## Стадии
### M0 — калибровка источника (ГЛАВНАЯ; GPU)
- Prep-энергия = упругость (e_stretch+e_bend+e_twist, band4d) − h·Σ_i(n_i·ẑ),
  БЕЗ измерительных зажимов (a,b выключены в prep). Релаксация батча
  (Ланжевен+отжиг ИЛИ квенч) → распределение n_mid по батчу.
- Наблюдаемая: гистограмма n_mid,z; фит формы 1+χ n_z (взвеш. МНК на
  cosθ-гистограмме); кривая **χ(h)** регистрируется; перекрёстно
  P(s=+|ẑ)=½+χ/4 (аналитика).
- **KILL M0:** χ(h)<3σ при ВСЕХ h≤h_max ⇒ стена «источник не поляризуется».
- Сетка h: 5 точек 0..h_max (h_max по гейту A3), оба режима.

### M1 — стиринг (после M0-прохода и моего go): кондиционир. (s,a), D_mech vs аналитика.
### M2 — телеграф-скан f_p на механич. ансамблях; нуль-множество {2}; аналитика рядом.

## Приоры архитектора (журнал): χ(h) калибруется 0.7; D_mech>0 0.4;
## полное механич. замыкание premise (M2 нуль-множ.{2}) 0.35;
## [стена конусного срезания 0.3 — СНЯТА аналитикой C3Bmech, из счёта убрана].

## Прогнозы M0 (A4/A5): отжиг поляризуется (χ(h_max)>3σ) 0.6;
## квенч замораживает частичный биас (0<χ_quench<χ_anneal) 0.55.

## Дисциплина: jax.devices() до прогона (GPU показан: CudaDevice id=0);
## smoke→full→§4.2→report; assert prereg↔JSON; сырьё→коммит→анализ.
## Открытый под-вопрос C2-J (квенч-стекло): регистрируем оба режима явно (A4/A5).
