# C3-L / L2 prereg FINAL (арена = совместный хордовый p=2, per addendum1 §3).
# Коммит ДО батареи L2. Наследует C3L_prereg.md (A1-A5) + addendum1.

## Совместный закон (арена): хорда p=2
P(s,t|a,b) = (1 − s·t·cosθ)/4, cosθ=a·b ⇒ E(a,b) = −cosθ, S_max = 2√2.
Факторизованная (LHV) реализация ЗАПРЕЩЕНА Беллом (S=2√2>2) ⇒ вопрос
фолиации ПРИНУДИТЕЛЕН.

## Механики
- **(i) preferred-foliation:** пара разрешается совместно по глобальному τ;
  фолиация n̂ — параметр. Полный (a,b)-закон доступен ⇒ воспроизводит
  E=−cosθ, S=2√2. Тест: оставляет ли ось n̂ наблюдаемый след?
- **(ii) frame-local «каждый первый»** (A3): локальные часы τ_A,τ_B.
  - space-like (а): рулёжка не успевает НИ у кого ⇒ совместный закон
    недоступен ⇒ каждый конец отвечает ЛОКАЛЬНО (λ~μ_χ + своя настройка),
    факторизованный отклик f_2(λ··) ⇒ LHV, S≤2.
  - time-like A→B (б) / B→A (в): первый разрешается локально, второй —
    по условному P(t|s,a,b)=P(s,t)/P(s) ⇒ реконструирует совместный ⇒ S=2√2.

## Фолиации (A2): 5 осей {ẑ, (ẑ+x̂)/√2, x̂, (ẑ−x̂)/√2, Haar-случайная
## (seed=20260721)}. N=2·10⁶.

## Прогнозы архитектора (журнал):
- **H-L2a'**: (ii) в space-like → LHV-ограничено (S≤2), форма = семейство
  отклика/треугольник — **0.7**; точный продукт (E=const=m_A m_B) — **0.15**.
- **H-L2b'**: (i) воспроизводит cosθ/S=2√2 ВСЮДУ И фолиации неразличимы
  (<2σ на всех 5 осях, N=2e6) — **0.65**.
- H-L2c: улики += порядко-зависимость коллапса (L1-развилка, C3L_L1_STOP_note).

## KILL / стоп-точки (немедленно):
1. Различимость фолиаций в (i) >2σ на ЛЮБОЙ оси = СТОП («детектор фрейма»:
   сенсация или баг — разбор до интерпретации).
2. (ii) даёт S>2 в space-like = СТОП (нарушение Белла без совместного
   закона = баг конструкции).
## Дисциплина: smoke→full→§4.2→report; аналитика рядом (E=−cosθ / cosθ·c);
## сырьё→коммит→анализ.
