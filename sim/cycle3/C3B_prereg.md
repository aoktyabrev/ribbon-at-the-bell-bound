# C3-B prereg (утверждён архитектором по мандату Артема 2026-07-20,
# редакция 77cc845 без правок). «Борн из причинности» внутренне —
# закрыть импорт партиально-запутанных ансамблей.

## СТАТУС-ВЕРДИКТ (после addendum1+2, hardening rerun c152c98):
# **internal derivation of the Born rule from no-signaling within the
# ribbon's steering class.** Клейм-дисциплина addendum1 выполнена:
# T1 (цепные/порядок, B7) ✓ + T2 (робастность на 5 деформациях меры,
# аналит. нуль-множ.={2} ∀, |аналит−числ|<2σ) ✓ + аналитика (аффинность
# f₂ ⇒ зазор Йенсена≡0 в p=2, алгебраически). T3 (F1) — ГРАНИЦА
# конструкции: F1 = прямой корр.-закон, стиринг-коллапс неприменим,
# импорт не закрывает (closes_import=false; прогноз 0.4 как записан).
# СТОП-вердикты прогона 19:39 — ложные (гейт квадратуры, см. addendum2).

## Прогнозы архитектора (в журнал при prereg-коммите)
F2 проходит B1-B3,B6,B7 — 0.6; F1 проходит — 0.4;
D(χ)>0 у F2 — 0.5; при D>0 замыкание (Δ=0 ⇔ p=2 по всем χ) — 0.75;
ловушка обобщённой защиты срабатывает (нуль-множество шире {2}
хотя бы при одном χ) — 0.15; стена B5 (D≡0 всеми семействами) — 0.3;
расхождение аналитика/численность >2σ где-либо — 0.1.

## Цель
Канон §3.2: no-signaling на партиально-запутанной рулёжке форсирует p=2,
НО ансамбли рулёжки импортированы из КМ (born2.py:20-24 «эксп. факт:
частично запутанные пары существуют»; {q,1−q} и {50/50 наклон} —
КМ state-steering). Закрыть импорт: асимметричный закон ленты
P(s,t|a,b,χ) должен ПОРОДИТЬ рулёжку ВНУТРЕННЕ, тогда no-signaling
даст p=2 без КМ-формул.

## Батарея свойств (ЯДРО; источник — born2/chord, НЕ КМ-формулы)
Закон P(s,t|a,b,χ) обязан удовлетворять, чтобы теорема замкнулась внутренне.
Каждое свойство — исполняемым assert'ом на numpy-батарее (упрочнение цикла 2).

(B1) РЕПИТАБЕЛЬНОСТЬ/собственные концы (born2 f(±1)∈{0,1}, chord.py:9-11
  вырожденные ветви): P(s=+|a,χ)→1 при n·a→+1, →0 при n·a→−1. Исходы ±1.
(B2) ЭКВАТОРИАЛЬНАЯ НЕсмещённость (born2 f(0)=1/2): при n·a=0 P(s=+)=1/2.
(B3) NO-SIGNALING/маргинал (born2 Батарея3 требование Δ=0, born2.py:35-39;
  chord.py:68): P(s|a,b,χ)=P(s|a,χ) НЕзависим от удалённой b; симметрично
  P(t|a,b,χ)=P(t|b,χ). ЦЕНТРАЛЬНАЯ связь.
(B4) СРЕДНЕ-ДОСТАТОЧНОСТЬ ⟺ АФФИННЫЙ отклик (born2 Батарея2, born2.py:8-14:
  два ансамбля с одним средним различимы ⟺ f нелинейна): ⟨отклик⟩ по
  ЛЮБОМУ ансамблю = отклик(⟨n·a⟩) ⇒ f аффинна в проекции c=n·a ⇒
  f=(1+c)/2 = Борн (p=2). Это внутренний форсер p=2.
(B5) ВНУТРЕННЯЯ ГЕНЕРАЦИЯ СТИРИНГА (несущая; ЗАКРЫВАТЕЛЬ импорта).
  Мера разнообразия: D(χ) = max по парам настроек Алисы (a,a') расхождения
  старших моментов (начиная с 3-го) рулёных ансамблей Боба при равенстве
  средних (равенство средних — с допуском, привязанным к численной точности
  батареи; пары с неравными средними в D не входят — они тестируют B3, не B5).
  B5 пройден ⇔ D(χ)>0 значимо на промежуточных χ. Регистрируемая ловушка
  «обобщённой защиты»: если разнообразие есть, но все ансамбли связаны
  симметрией, сохраняющей класс правил шире {p=2}, — фиксируется исход
  «причинность отбирает класс, не точку»; проверка: телеграф-скан по (p,χ)
  на СГЕНЕРИРОВАННЫХ ансамблях, множество нулей Δ по p при каждом χ — в отчёт.
(B6) ГРАНИЧНЫЕ по χ: χ=0 → произведение (E=0, рулёжки нет, разложения
  тривиальны); χ→χ_max → максимальная (анти)корреляция; диверсность B5
  существует при промежуточном χ; E(χ) монотонна при фикс θ.
(B7) ЦЕПНЫЕ/ПОРЯДОК (репитабельность под композицией): пост-измеренческое
  χ-обновление ассоциативно для последовательных измерений; повтор a →
  тот же s; совместный закон порядко-консистентен (χ несёт причинную
  асимметрию, но наблюдаемый no-signaling держится в любом порядке).

ТЕОРЕМА замыкается ВНУТРЕННЕ ⟺ лента даёт B1,B2,B5,B6,B7 (рулёжка из своей
χ-структуры) И тогда no-signaling B3+B4 форсирует аффинность = Борн (p=2).
Импорт #2 закрыт ИМЕННО B5.

## Семейства-кандидаты (порядок: F2 первичное, F1 вторичное, F3 резерв)
- **F2 смещённая мера скрутки источника (ПЕРВИЧНОЕ):** плотность микрооси λ
  у источника = (n·λ)_+ · (1 + χ·(m·λ)) — смещение КС-меры (chord.py:43-52)
  вектором асимметрии m(χ). χ рулит форму разложения при фикс среднем.
  Геом. обоснование первичности: структура «антикорреляция вдоль оси +
  ослабленная поперечная + смещённые маргиналы» NATIVE для смещённой меры
  ориентаций (фаворит архитектора, приор 0.55).
- **F1 взвешенная хорда (ВТОРИЧНОЕ):** P(s,t|a,b) ∝ |s·√w_a(χ)·a − t·√w_b(χ)·b|^p,
  w(χ) — асимметричные веса концов (χ смещает баланс). Обобщает chord.py
  (симметрия w_a=w_b=1); χ вводит асимметрию концов.
- **F3 двухпараметрическое смещение×вес (РЕЗЕРВ):** активируется ТОЛЬКО при
  провале B6 обоими F1,F2 И отдельным addendum'ом (не по умолчанию).

## KILL-критерии (регистрируются)
- Провал ЛЮБОГО из B1,B2,B6,B7 семейством ⇒ семейство ОТПАДАЕТ.
- B5-провал (нет рулёжной диверсности внутренне) семейством ⇒ у B4 нет
  предмета ⇒ семейство НЕ закрывает импорт.
- Провал ВСЕХ зарегистрированных семейств по B5 ⇒ СТЕНА: рулёжка
  невыводима внутренне в классе ⇒ импорт неустраним ⇒ фиксируется
  ИСХОДОМ (не провалом); канон §3.2 остаётся честным «within ansatz».
- ТЕСТ ЕДИНСТВЕННОСТИ p=2: телеграф-скан Δ(p,χ) по (p,χ)-сетке (per A4):
  p ∈ {1, 1.5, 1.75, 2, 2.25, 2.5, 3}; χ — 5 точек от продукта до максимума
  (края + 3 промежуточные); N=2e6/ячейку (именованный эстиматор σ_bin).
  Критерий нуля: |Δ(p=2,χ)| < 2σ_bin И Δ(p,χ) > 5σ_bin для всех соседей
  |p−2| ≥ 0.25 при каждом χ с D(χ)>0. Где есть аналитическая Δ-формула
  (класс born2) — аналитика ОБЯЗАТЕЛЬНА рядом с численностью, расхождение
  >2σ = стоп-разбор. Провал критерия = баг батареи ИЛИ p=2 не форсируется.
  ТРЕБОВАНИЕ ВНУТРЕННОСТИ: телеграф-скан использует ИСКЛЮЧИТЕЛЬНО ансамбли,
  порождённые семейством, прошедшим B5. КМ-ансамбли (born2-структура
  {q,1−q} / tilted) в скане ЗАПРЕЩЕНЫ — допускаются только как независимая
  контрольная колонка «QM-reference» отдельной таблицей. Замыкание:
  ∃χ: D(χ)>0 ∧ (Δ(p,χ)=0 ⇔ p=2) ⇒ теорема внутренняя.

## Точность/бэкенд
numpy-батареи (CPU, MC N≥2e6 как born2.py:2). БЕЗ GPU/науки ленты на этапе 0.
Батарея = numpy, детерминированный seed, ассерты B1-B7 исполняемы.

## Вопросы Артему — ОТВЕТЫ АРХИТЕКТОРА (этап 0.6, дословно)
Q1. Приоры C3-B — подтвердить как зарегистрированные до батарей?
A1. Подтверждаю: приоры зарегистрированы до батарей, версия этапа 0.5
финальна, дальнейшие правки приоров — только с причиной и timestamp.

Q2. χ-параметризация; физ-смысл в ленте или феноменология?
A2. χ — СКАЛЯР (баланс концов) + фиксированная ось m как параметр,
эквивариантность по совместному повороту (χ, m, a, b). Феноменологичность
на этапе бумаги ДОСТАТОЧНА: внутренность теоремы требует, чтобы рулёные
ансамбли порождались собственным законом семейства, а не механической
реализацией χ в упругой ленте. Механическая реализация (несимметричный
источник в модели цикла 2) — ОТДЕЛЬНЫЙ зарегистрированный долг
C3-B-mech, открывается после теоремы, не блокирует её.

Q3. B5-диверсность — два разложения или континуум?
A3. Двух разложений ДОСТАТОЧНО для детекции Δ>0 (скан); для
аффинного форсинга B4 используется естественный континуум — каждая
настройка Алисы a ∈ S² даёт своё разложение, континуум приходит
бесплатно с законом. Регистрируем оба уровня: скан-минимум = 2;
лемма форсинга формулируется на континууме по a.

Q4. Телеграф-скан: порог Δ и N?
A4. Строже born2, с именованным эстиматором: N = 2e6 на ячейку;
сетки регистрируются: p ∈ {1, 1.5, 1.75, 2, 2.25, 2.5, 3},
χ — 5 точек от продукта до максимума (края + 3 промежуточные).
Критерий нуля: |Δ(p=2,χ)| < 2σ_bin И Δ(p,χ) > 5σ_bin для всех
соседей |p−2| ≥ 0.25 при каждом χ с D(χ)>0. Где семейство допускает
аналитическую Δ-формулу (класс born2) — аналитика обязательна
рядом с численностью, расхождение >2σ = стоп-разбор.

Q5. Стена по B5 — go на C3-L или стоп-фиксация?
A5. Стена по B5 = стоп-фиксация С3-B как исхода (публикуемого),
НЕ провала. C3-L НЕ является альтернативным закрывателем импорта:
она закрывает дыру #5 (фолиация), не #2 (стиринг) — подмена
дыр запрещается этой строкой. При стене C3-B: C3-L и C3-G
продолжаются своими треками без переприоритизации явочным порядком.
