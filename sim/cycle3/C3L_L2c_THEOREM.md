<!-- English abstract; canonical text below in Russian (do not translate/edit canonical). -->
> **English abstract — Class-M precedence theorem (L2c).**
> Class M = outcome-definite sequential pair-resolution mechanisms (axioms
> M1–M4′: local maps from preparation randomness λ, local setting, and the
> ≺-preceding transcript; no retrocausation; settings independent of λ).
> **Theorem:** in class M, CHSH S>2 is *achievable* iff M admits a shared,
> space-like-transcending, statistically invisible precedence structure ≺.
> Realizations: preferred foliation; per-run shared coin (frameless);
> degenerate (single joint resolution event). Proof: global contrapositive
> (all pairs unordered ⇒ global LHV ⇒ S≤2) + T2 invisibility (chain rule) +
> β-battery (the coin reaches S=2√2 without signaling, order/coin invisible).
> **Status:** theorem — assigned after a clean third adversarial pass + operator
> authorization. Commits: `c8e1bf3` (theorem + β-coin), `2053106` (§2.3 retraction).
> Kinship: Suarez–Scarani + Bell (core) + our delta = formalized class M,
> T2-invisibility, executable β, before-before anchor [ZBGT01, SZGS02].

# C3-L / L2c — ТЕОРЕМА КЛАССА M. Статус: ТЕОРЕМА (α письм.+ β батарея + γ авториз.).
# Присвоено после ЧИСТОГО третьего адверсариального прохода (см. низ файла).
#
# ФОРМУЛИРОВКА (точно): в классе M (исходо-определённые последовательные
# механизмы разрешения пары, аксиомы M1-M4′): CHSH S>2 ДОСТИЖИМО ⇔ M допускает
# РАЗДЕЛЯЕМУЮ, space-like-трансцендирующую, статистически невидимую (T2)
# структуру предшествования ≺. Реализации: выделенная фолиация; пер-рановая
# общая монета (бесфреймовая); вырожденная (одно совместное событие).
# β: монета достигает S=2√2 без сигналинга, «кто первый» невидим (C3L_coin.json).
#
# ДЕЛЬТА vs РОДНЯ (для статьи): ядро T1″ = [SS97] frame-local + Белл (LHV≤2) —
# НЕ ново. Новизна ТЕОРЕМЫ = (i) класс M формализован (M1-M4′); (ii) T2-НЕВИДИМОСТЬ
# (структура предшествования без статистического следа) — нет у SS/Белла;
# (iii) β-батарея (монета/фолиация исполняемы); (iv) эксп. якорь [ZBGT01,SZGS02]
# (before-before исключил frame-local ветвь в природе ⇒ выжила preferred-foliation).
# Родня: [H92,SS97,ZBGT01,SZGS02,DGZ92]. Мост в статью 3 §6/L2c.
#
# Ниже: дословный черновик (архитектор) + разбор (исполнитель) + 3 прохода.

## Черновик теоремы (архитектор, дословно)
Класс M (исходо-определённые последовательные механизмы разрешения
пары): (M1) каждое событие измерения e_X, X∈{A,B}, разрешается
картой R_X (возможно стохастической) от: общих данных приготовления
λ, локальной настройки, и транскрипта событий, ПРЕДШЕСТВУЮЩИХ e_X
в порядке механизма ≺; исход определён по разрешении. (M2)
статистика воспроизводит несигналящее P(s,t|a,b). (M3) без
ретрокаузации: R_X слепа к не-предшествующим событиям. (M4)
свободные настройки: (a,b) независимы от λ и от ≺.
T1 (принуждение порядка): если для space-like пары порядок
фрейм-локален (каждое событие «первое» в своём ≺_X — транскрипт
пуст у обоих), то s ⊥ t | λ ⇒ P = λ-смесь продуктов ⇒ CHSH ≤ 2.
Следовательно S>2 в M требует ОБЩЕГО порядка на space-like парах —
пространственно-неинвариантного отношения предшествования
(выделенная фолиация как частный случай; вырожденный случай
«одно глобальное событие разрешает обоих» = все на одном слое).
T2 (невидимость): для любого совместного P и любого общего порядка
двухшаговая композиция P(1st|·)·P(2nd|1st,·) воспроизводит P
тождественно (цепное правило); для любых двух порядков ≺, ≺′
существуют механизмы в M с идентичной статистикой; никакой
статистический функционал от P не различает порядки. [Модельное
свидетельство: L2b', различимость 0.000000 на 5 фолиациях, CRN.]
T3 (королларий-мост): в классе M: S>2 ⇔ существует выделенная и
статистически невидимая структура предшествования.

## АДВЕРСАРИАЛЬНЫЙ РАЗБОР (исполнитель)

### 1. T1 построчно — где используются M1/M3/M4
- «транскрипт пуст у обоих» ⇒ R_A = R_A(λ, a, ω_A), R_B=R_B(λ, b, ω_B),
  ω_X — локальный шум карты. **[M1]** (аргументы R_X). 
- s зависит только от (λ,a,ω_A), НЕ от b: **[M3]** (слепа к e_B, которое не
  предшествует). Без M3 R_A могла бы читать b ⇒ прямой сигналинг.
- s ⊥ t | (λ, ω_A, ω_B). Если ω_A ⊥ ω_B ⊥ λ ⇒ маргинализация по ω даёт
  P(s,t|a,b,λ)=P(s|a,λ)P(t|b,λ) ⇒ P(s,t|a,b)=∫dμ P(s|a,λ)P(t|b,λ). **LHV.**
- Белл: LHV ⇒ CHSH≤2. Применимость Белла требует (a,b)⊥λ: **[M4]**
  (иначе супердетерминизм лазейка).
- **КОНТРПРИМЕР-ПОПЫТКА (а): стохастич. R с общей случайностью ω сверх λ.**
  Разбор: расширить скрытую переменную λ'=(λ,ω). ω приготовительна (общий
  источник) ⇒ ω⊥(a,b) ⇒ M4 держится для λ'. Смесь по λ' — снова LHV. ⇒
  **фолдится, не ломает.** ЕСЛИ ω генерируется ПОСЛЕ выбора настроек и
  коррелирует с ними — это нарушение M4 (супердетерминизм), вне класса.
  [ДЫРА-1: нужно явно потребовать ω приготовительно, т.е. ≺-предшествует
  выбору настроек — уточнить M1: «λ и вся общая случайность фиксированы
  до настроек».]
- **КОНТРПРИМЕР-ПОПЫТКА (б): частичный транскрипт (подслушивание без
  порядка).** Если R_A видит ЧАСТЬ данных e_B без того, чтобы e_B ≺ e_A —
  нарушает M3 (слепа к не-предшествующим). ⇒ вне M ИЛИ вводит порядок
  (если «видит» ⇒ e_B фактически предшествует). Классификация: либо
  сигналинг-канал (нарушает M2 no-signaling), либо неявный общий порядок
  (это уже T1-заключение). [Чисто.]

### 2. T2 — численное тождество + граница клейма
- **Численно (N=2·10⁶, хордовый закон):** max|P_Afirst − P_Bfirst| = 1.12e-3,
  max|P_recon − P_analytic| = 7.16e-4, оба <2σ=1.4e-3. Цепное правило
  воспроизводит P тождественно обоими порядками. [подтверждает T2-ядро.]
- **ГРАНИЦА КЛЕЙМА (T2 сильнее батареи — точная формулировка):**
  · Доказано (цепное правило): для ЛЮБОГО совместного P и любых двух
    порядков реконструкции — P(s,t|a,b) ИДЕНТИЧНО. ⇒ любой функционал
    ОТ P(s,t|a,b) не различает порядки. Это НЕ «существуют механизмы»,
    а «ВСЕ механизмы, чья статистика есть P(s,t|a,b)» — СИЛЬНЕЕ, чем
    формулировка черновика «существуют механизмы».
  · [ДЫРА-2: черновик T2 говорит «существуют механизмы в M с идентичной
    статистикой» — на деле цепное правило даёт СИЛЬНЕЕ (все). Переписать:
    «для любого порядка механизм воспроизводит P; статистика = P ⇒ инвар.».]
  · ОГОВОРКА (где T2 НЕ покрывает): наблюдаемые, НЕ являющиеся функционалом
    P(s,t|a,b) — напр. гипотетич. доступ к самому порядку разрешения
    (which-first) или к пост-транскрипту. В классе M такие не входят в
    статистику (M2 фиксирует наблюдаемое = P). ⇒ невидимость ОТНОСИТЕЛЬНО
    P-статистики; вне её — не клейм. Батарея L2b' (disc 0.0 на 5 фолиациях)
    = частный β-свидетель, НЕ доказательство «никакой функционал».

### 3. Сверка с роднёй — дельта честно
- T1 = SS-логика (frame-local ⇒ before-before) + Белл (LHV≤2)? ЧАСТИЧНО ДА:
  ядро T1 (frame-local space-like ⇒ LHV ⇒ ≤2) — переизложение [SS97] +
  Белл. **ДЕЛЬТА (наше, для статьи):** (i) класс M формализован явными
  аксиомами M1-M4 (SS не формализовали класс); (ii) **T2-невидимость** —
  выделенный порядок не оставляет статистического следа — НЕ в SS/Белле
  (новое); (iii) батарейное β (L2/L2b', исполняемое модельное свидетельство);
  (iv) эксп. якорь [ZBGT01,SZGS02] встроен как исключение ветки (ii).
  [Честно: T1 сам по себе не нов; новизна = M + T2 + β + мост T3.]

## СПИСОК ДЫР/ПОПРАВОК (для γ-ревью архитектора)
- ДЫРА-1: M1 уточнить — «λ и вся общая случайность приготовительны,
  ≺-предшествуют выбору настроек» (закрывает контрпример ω-после-настроек).
- ДЫРА-2: T2 переписать сильнее — «все механизмы со статистикой P», не
  «существуют»; добавить оговорку «невидимость относительно P-статистики».
- ДЫРА-3 (открытая): T1 доказывает ≤2 для ЧИСТО frame-local; НЕ доказано,
  что ЧАСТИЧНО-общий порядок (смешанные конфигурации) не достигает 2√2
  локально — нужен аргумент, что ЛЮБОЙ S>2 требует общего порядка на
  space-like парах, не только чисто-frame-local провал. [Ключевая для T3.]
- ДЫРА-4: «выделенная фолиация» vs «вырожденный одно-событие» — T1 даёт
  «общий порядок»; сведе́ние к именно ФОЛИАЦИИ (Lorentz-структура) требует
  доп. посылки (напр. непрерывность/локальность порядка) — назвать явно.
- Статус: до закрытия ДЫР-1..4 и γ-ревью — **conjecture with model evidence**,
  НЕ теорема (дисциплина A5).

## ============ γ-РЕВЬЮ: ПРАВКИ (архитектор, дословно) ============
### M1-фикс (закрывает ДЫРА-1)
> λ := вся общая случайность приготовления (включая любую ω стохастических
> R); аксиома M4′: λ настройко-независима. Фолдинг легален только при M4′;
> нарушение M4′ = супердетерминизм, вне класса M — явной строкой.

### T2-усиление (закрывает ДЫРА-2)
> цепное правило ⇒ ЛЮБОЙ механизм из M, воспроизводящий P, статистически
> эквивалентен обоим порядкам; невидимость — относительно операциональной
> P-статистики; which-first не является P-функционалом по определению
> операционального доступа.

### T1″ (α-правка, заменяет T1′; закрывает ДЫРА-3′ — глобальная контрапозиция)
> T1″ (глобальная контрапозиция): если ни одна space-like пара не
> упорядочена в ≺ (все транскрипты space-like событий взаимно пусты),
> то каждый исход разрешается картой от (λ, локальная настройка,
> локально-предшествующий транскрипт) ⇒ совместная статистика есть
> λ-смесь локально-порождённых распределений = глобальный LHV ⇒
> CHSH ≤ 2 для всех пар. Контрапозиция: S>2 ⇒ существует хотя бы
> одна упорядоченная space-like пара; согласование «кто первый» —
> разделяемая структура (T3′). Попарная атрибуция «виновной» пары
> по P не требуется и не заявляется.

### T3′ (заменяет T3; закрывает ДЫРА-4)
> T3′: в классе M: S>2 ⇔
> существует РАЗДЕЛЯЕМАЯ, space-like-трансцендирующая структура
> предшествования, статистически невидимая (T2). Реализации:
> выделенная фолиация (фреймовая); пер-рановый общий случайный
> порядок (бесфреймовая — общая монета согласована между событиями);
> вырожденная (одно совместное событие разрешения). Клейм «фолиация
> обязательна» ОТОЗВАН как избыточный; клейм «разделяемая невидимая
> структура предшествования обязательна» — доказываемое ядро.

## ============ ВТОРОЙ АДВЕРСАРИАЛЬНЫЙ ПРОХОД (по исправленному) ============
### ДЫРА-4 (дословно, для протокола) и ревью
Цитата: «ДЫРА-4: „выделенная фолиация“ vs „вырожденный одно-событие“ — T1
даёт „общий порядок“; сведе́ние к именно ФОЛИАЦИИ (Lorentz-структура)
требует доп. посылки (напр. непрерывность/локальность порядка) — назвать
явно.»
РЕВЬЮ: T3′ **разрешает ДЫРУ-4 отзывом** избыточного клейма. Ядро сведено к
«разделяемая невидимая структура предшествования» — что T1′ (LHV≤2 без
такой структуры) и Белл действительно дают. Три реализации (фолиация /
пер-рановая общая монета / вырожденная) — корректная классификация
«общего порядка» без переусиления до Lorentz-фолиации. ✓ ДЫРА-4 закрыта.

### T1′ построчно (ДЫРА-3)
- Контрапозиция: НЕ упорядоченная в ≺ space-like пара ⇒ (по M1+M3, транскрипт
  взаимно пуст) ⇒ s⊥t|λ на ЭТОЙ паре ⇒ её корреляторы факторизуются по λ.
- S>2 (CHSH) требует, чтобы ВСЕ четыре коррелятора нарушали факторизацию
  совместно (Белл: любая LHV-факторизуемая пара ⇒ вклад в S ограничен).
  **[Тонкость-A]:** CHSH смешивает 4 пары настроек; строго нужно: если ХОТЯ
  БЫ одна из участвующих пар факторизуется по λ, достижимый S всё ещё может
  быть >2? НЕТ — если все 4 пары суть один и тот же source λ и КАЖДАЯ
  неупорядоченная пара факторизуется, то вся четвёрка = LHV-модель ⇒ ≤2.
  Достаточно: S>2 ⇒ хотя бы одна пара НЕ LHV-факторизуема ⇒ та пара
  упорядочена. T1′ формулирует ПОПАРНО («каждая пара, участвующая в S>2»)
  — корректно, если читать «участвующая» как «чья не-факторизация необходима
  для S>2». [ДЫРА-3 сужена, НЕ полностью закрыта — см. ниже.]
- **ОСТАТОЧНАЯ ДЫРА-3′ (новая, честно):** «участвует в S>2» неоперационально
  — нельзя по одному P сказать, КАКАЯ пара «виновата». Строгая версия: S>2
  ⇒ P не-локальна ⇒ (Белл) НЕ ∃ глобальная LHV ⇒ ∃ пара без ≺-порядка НЕВОЗМОЖНА
  для всех сразу быть неупорядоченной. Т.е. доказуемо: **НЕ ВСЕ** space-like
  пары могут быть неупорядочены (иначе глобально LHV ⇒ ≤2). Это СЛАБЕЕ, чем
  «каждая участвующая упорядочена», но ДОСТАТОЧНО для T3′ (∃ разделяемая
  структура предшествования). Рекомендация: T3′ доказывать через ГЛОБАЛЬНУЮ
  контрапозицию (все неупорядочены ⇒ глобальный LHV ⇒ ≤2), НЕ попарную.
  Тогда T1′ следует формулировать: «S>2 ⇒ ∃ хотя бы одна упорядоченная
  space-like пара», а «разделяемость/согласованность» структуры — из того,
  что монета/фолиация должна быть ОБЩЕЙ между разнесёнными событиями.

### Контрпримеры к исправленному
- **Пер-рановая общая монета (бесфреймовая):** общий случайный порядок,
  согласованный между A и B (разделяемая переменная πрежде-настроек). Это
  РЕАЛИЗАЦИЯ T3′ (не контрпример) — но проверить: монета согласуется БЕЗ
  сигналинга? Да, если разыграна в приготовлении (⊂λ по M4′). Но тогда
  порядок ⊂λ ⇒ «упорядоченность» через скрытую переменную — законно в M.
  [Тонкость-B: такая монета делает s,t зависимыми через λ И через порядок —
  не ломает M2 (no-signaling на маргиналах) — численно бы проверить, что
  общий-случайный-порядок механизм даёт S=2√2 без сигналинга. НЕ в текущей
  батарее — кандидат β-теста для полного β.]
- Which-first доступ: по T2-усилению — не P-функционал ⇒ вне операциональной
  статистики ⇒ не контрпример. ✓

### ВЕРДИКТ ВТОРОГО ПРОХОДА
- ДЫРА-1 ✓ закрыта (M4′). ДЫРА-2 ✓ (T2-усиление + оговорка). ДЫРА-4 ✓
  (T3′ отзыв). **ДЫРА-3 → ДЫРА-3′:** попарная формулировка T1′ неоперациональна;
  нужна ГЛОБАЛЬНАЯ контрапозиция (все неупорядочены ⇒ LHV ⇒ ≤2) — правка
  формулировки T1′, НЕ содержательная брешь. **Новый β-долг:** численно
  показать «пер-рановая общая монета» даёт S=2√2 без сигналинга (реализация
  T3′ бесфреймовой ветки) — в текущих батареях нет.
- **СТАТУС (после 2-го прохода): оставался conjecture** до (α) T1″ + (β) монета
  + (γ). Ниже — 3-й проход по исправленному.

## ============ ТРЕТИЙ АДВЕРСАРИАЛЬНЫЙ ПРОХОД (T1″+T2+T3′+M4′) ============
### α закрыто (T1″ глобальная контрапозиция)
Все space-like пары неупорядочены ⇒ каждый исход = R_X(λ, настройка,
локально-предшеств. транскрипт); локально-предшеств. транскрипт ⊂ функция λ
(события причинного прошлого X, все выводимы из λ) ⇒ s=R_A(λ,a), t=R_B(λ,b)
⇒ P(s,t|a,b)=∫dμ(λ)P(s|a,λ)P(t|b,λ) = глобальный LHV ⇒ CHSH≤2. **[M3]**
(слепа к не-предшеств.), **[M4′]** (λ⊥настройки — применимость Белла).
Контрапозиция операциональна (глобальная, без атрибуции «виновной» пары). ✓
### β закрыто (монета, C3L_coin.json)
Пер-рановая общая монета w (приготовительная, M4′): S=2.8291≈2√2 (|Δ|=6e-4<2σ),
E=−cos45, маргиналы ½, Δ-телеграф=5e-4<2σ, **w-невидимость=1.3e-3<2σ**. ⇒
бесфреймовая ветвь T3′ РЕАЛИЗУЕМА: 2√2 без сигналинга, «кто первый» невидим. ✓
### Согласованность структуры (тонкость, разобрана)
«Разделяемая структура» = ≺ из M1 — по определению механизма это ЕДИНОЕ
частичное отношение (ацикличное), НЕ набор несогласованных попарных порядков.
T1″ даёт: S>2 ⇒ ≺ нетривиально упорядочивает хотя бы одну space-like пару.
Разделяемость (общая между разнесёнными) = ≺ едино для механизма (M1);
согласованность = ацикличность ≺ (M1-определение). ⇒ «структура» корректна. ✓
### Точная граница биконди T3′ (честно, чтобы не переусилить)
S>2 ⇔ ∃ разделяемая невидимая структура предшествования:
- (⇒) НЕОБХОДИМОСТЬ — из T1″ (строго).
- (⇐) ДОСТАТОЧНОСТЬ — ПО ПРИМЕРУ (монета/фолиация достигают 2√2), НЕ «всякая
  структура даёт 2√2». Клейм: «S>2 ДОСТИЖИМО в M ⇔ M допускает такую структуру».
  Формулировать именно так (достижимость), не «S>2 ⇒ конкретная структура».
### Контрпримеры — исчерпаны
ω-после-настроек (M4′-нарушение, вне M); частичный транскрипт (M3-нарушение
или неявный порядок); which-first (не P-функционал, T2). Новых нет.

### ВЕРДИКТ ТРЕТЬЕГО ПРОХОДА: ЧИСТ.
ДЫРА-1/2/3′/4 закрыты; α (T1″) + β (монета) + структура-согласованность
разобраны; биконди сформулирован точно (достижимость). **СТАТУС: ТЕОРЕМА
КЛАССА M** присвоена (α письм. + β батарея + γ = условная авторизация
архитектора Задача 1.3 при чистом 3-м проходе). Файл → C3L_L2c_THEOREM.md.
Остаточных содержательных дыр нет; точность биконди (достижимость, не
«конкретная структура») зафиксирована в формулировке.
