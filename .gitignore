# окружение
sim/.venv/
__pycache__/
*.pyc
.pytest_cache/

# результаты: трекаем только report.md (SPEC §6), не тяжёлые артефакты
sim/results/**/*.png
sim/results/**/*.npz

# Windows ADS-мусор от скачанных файлов
*:Zone.Identifier
sim/results/*.log

# PDF-сборки — генерируются paper/build_pdf.sh
paper/pdf/*.log
paper/pdf/*.aux
paper/pdf/*.tex
